package com.example.domain.model

/**
 * Domain model package placeholder for raw and calculated models.
 * Raw data from entities serves as the single source of truth.
 */
data class WorkSummary(
    val date: String,
    val workTimeMinutes: Int
)
